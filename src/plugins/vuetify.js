import Vue from 'vue';
import Vuetify from 'vuetify/lib';
import Autocomplete from 'v-autocomplete'

// imports Vuetify plugin and loads it into Vue instance
Vue.use(Vuetify);
Vue.use(Autocomplete);

export default new Vuetify({
});
